name: Yad2 Haifa Property Monitor

on:
  schedule:
    # Run every 10 minutes
    - cron: '*/10 * * * *'
    # Daily status report at 9 AM Israel time (6 AM UTC)
    - cron: '0 6 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Download previous database
      uses: actions/download-artifact@v4
      with:
        name: yad2-database
        path: .
      continue-on-error: true  # Don't fail if no previous database exists
      
    - name: Check database status
      run: |
        echo "🔍 Checking database files..."
        ls -la *.db || echo "No .db files found"
        if [ -f "yad2_properties.db" ]; then
          echo "✅ Found previous database from artifact"
          sqlite3 yad2_properties.db "SELECT COUNT(*) as count FROM properties;" || echo "Database query failed"
        elif [ -f "github_yad2_properties.db" ]; then
          echo "🔄 Using GitHub baseline database"
        else
          echo "🆕 Starting with empty database"
        fi
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Advanced Yad2 monitor with GitHub persistence
      env:
        GMAIL_EMAIL: ${{ secrets.GMAIL_EMAIL }}
        GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        EMAIL_RECIPIENT_1: ${{ secrets.EMAIL_RECIPIENT_1 }}
        EMAIL_RECIPIENT_2: ${{ secrets.EMAIL_RECIPIENT_2 }}
      run: |
        echo "🏠 Starting GitHub Advanced Yad2 monitoring with persistence..."
        python github_advanced_monitor.py
        
    - name: Upload database for next run
      uses: actions/upload-artifact@v4
      if: always()  # Upload even if monitoring had issues
      with:
        name: yad2-database
        path: yad2_properties.db
        retention-days: 30
        
    - name: Send daily status (if scheduled)
      if: github.event.schedule == '0 6 * * *'
      env:
        GMAIL_EMAIL: ${{ secrets.GMAIL_EMAIL }}
        GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        EMAIL_RECIPIENT_1: ${{ secrets.EMAIL_RECIPIENT_1 }}
        EMAIL_RECIPIENT_2: ${{ secrets.EMAIL_RECIPIENT_2 }}
      run: |
        echo "📊 Sending daily status report..."
        python status_reporter.py